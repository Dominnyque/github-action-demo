name: 03 - Workflows Runner

on: push

jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show OS
              run: |
                  echo "This job is running onubuntu runner."
                  echo "Ubuntu runner OS is ${{ runner.os }}."

